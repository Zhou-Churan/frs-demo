# MySQL密码（默认root）
DB_PASSWORD=root

# Redis密码（默认无密码，如需密码请取消docker-compose.yml中redis的command注释）
REDIS_PASSWORD=

# 华为云FRS（必填）
HUAWEICLOUD_SDK_AK=你的AK
HUAWEICLOUD_SDK_SK=你的SK

# JWT密钥（可选，有默认值）
JWT_SECRET=frs-attendance-system-default-secret-key-2026